{
  "name": "terminal-template",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "ex": "bun run examples/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "publish": "npm run build && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "bun-types": "latest",
    "ts-type-helper": "^0.3.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "type": "module"
}
